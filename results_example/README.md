# Results Example

This directory is reserved for tiny validation fixtures and expected outputs.

Do not place full sequencing outputs here. Large or private data should stay outside the repository and be referenced from `config/samples.tsv`.
